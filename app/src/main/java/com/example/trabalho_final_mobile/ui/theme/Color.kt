package com.example.trabalho_final_mobile.ui.theme

import androidx.compose.ui.graphics.Color

// ============== PALETA MODERNA & MINIMALISTA ==============
// Inspirada em apps como Hopper / Airbnb / Skyscanner
// Foco: tons neutros + accent vibrante (teal moderno)

// --- Accent principal: Teal moderno ---
val Teal600 = Color(0xFF0F766E)  // Primary forte
val Teal500 = Color(0xFF14B8A6)  // Primary médio (botões, links)
val Teal400 = Color(0xFF2DD4BF)  // Primary claro (highlights)
val Teal100 = Color(0xFFCCFBF1)  // Primary container
val Teal50  = Color(0xFFF0FDFA)  // Surface tint

// --- Accent secundário: Coral suave (para Negócios) ---
val Coral600 = Color(0xFFE85D5D)
val Coral500 = Color(0xFFFF6B6B)
val Coral100 = Color(0xFFFFE0E0)
val Coral50  = Color(0xFFFFF5F5)

// --- Neutros (modo claro) ---
val Neutral0   = Color(0xFFFFFFFF)  // Branco puro
val Neutral50  = Color(0xFFFAFAFA)  // Background
val Neutral100 = Color(0xFFF5F5F5)  // Surface variant
val Neutral200 = Color(0xFFE5E5E5)  // Borders
val Neutral300 = Color(0xFFD4D4D4)  // Outline
val Neutral400 = Color(0xFFA3A3A3)  // Disabled / placeholder
val Neutral500 = Color(0xFF737373)  // Texto secundário
val Neutral700 = Color(0xFF404040)  // Texto importante
val Neutral900 = Color(0xFF171717)  // Texto principal

// --- Neutros (modo escuro) ---
val DarkBg        = Color(0xFF0A0A0A)
val DarkSurface   = Color(0xFF171717)
val DarkSurface2  = Color(0xFF262626)
val DarkBorder    = Color(0xFF404040)

// --- Estados ---
val ErrorRed   = Color(0xFFDC2626)
val ErrorBg    = Color(0xFFFEF2F2)
val SuccessGreen = Color(0xFF16A34A)
